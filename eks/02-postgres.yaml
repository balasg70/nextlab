apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: perf-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_USER
              value: appuser
            - name: POSTGRES_PASSWORD
              value: apppass
            - name: POSTGRES_DB
              value: contactdb
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: perf-test
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
